name: Unlock GitHub Actions Hero

on:
  push:
    branches: [ main ]

jobs:
  unlock:
    runs-on: ubuntu-latest
    steps:
      - name: Echo para desbloquear el logro
        run: echo "Hola GitHub!"
